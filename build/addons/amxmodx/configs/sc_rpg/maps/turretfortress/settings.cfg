;	Available commands:
;		disable	<bool>			// Plugin fully blocked, nothing will be loaded
;		block_map <bool>		// Blocks the map from any EXP earning, skills can still be used
;		cap_exp <int>			// Sets the amount of EXP the player can earn
;		cap_jump <int>			// Sets amount of jumps the player can do
;		regen_ammo <int>		// Sets the timer for the ammo regen
;		regen_weapon <int>		// Sets the timer fo the weapon randomizer

disable false
block_map false
cap_exp 2500
cap_jump 1